// package.json
{
  "name": "my-netlify-project",
  "version": "1.0.0",
  // ... other properties
  "dependencies": {
    "axios": "^1.0.0" // Use the latest stable version
    // ... other dependencies
  }
}
